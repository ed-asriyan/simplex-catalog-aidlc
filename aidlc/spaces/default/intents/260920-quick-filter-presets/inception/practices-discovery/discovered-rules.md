# Discovered Rules — DRAFT

> **DRAFT — pending the human interview.** `## Mandated` and `## Forbidden`
> only ever hold hard constraints the human has explicitly stated
> (mirroring the `project.md` promotion targets: `ALWAYS ...` /
> `NEVER ...`, affirmed at the practices-discovery gate). Nothing
> qualifies yet: git history, CI/CD configuration, and `package.json`
> tooling are useful evidence for the proposed defaults in
> `team-practices.md`, but inferred conventions are not the same as a
> human-affirmed hard constraint, so neither section is populated before
> the interview runs.

## Mandated

<!-- Populated only from explicit human statements made during this
     stage's interview. Format once affirmed: ALWAYS [behavior] (affirmed
     [date]) — matches the promotion format used in
     aidlc/spaces/default/memory/project.md. -->

None yet.

## Forbidden

<!-- Populated only from explicit human statements made during this
     stage's interview. Format once affirmed: NEVER [behavior] (affirmed
     [date]) — matches the promotion format used in
     aidlc/spaces/default/memory/project.md. -->

None yet.
